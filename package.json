{
    "name": "shogun-packages",
    "private": true,
    "workspaces": [
        "packages/*",
        "docs"
    ],
    "scripts": {
        "typecheck": "bun --cwd packages/prototype-extensions typecheck && bun --cwd packages/utils typecheck && bun --cwd packages/common typecheck && bun --cwd packages/webkit-native-bridge typecheck && bun --cwd packages/bun-server typecheck",
        "docs:dev": "bun --cwd docs dev",
        "docs:build": "bun --cwd docs build",
        "docs:preview": "bun --cwd docs preview",
        "docs:deploy": "wrangler deploy"
    },
    "dependencies": {
        "wrangler": "^4.120.0"
    }
}
